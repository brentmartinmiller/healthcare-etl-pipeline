# Copy to .env and fill in values
DATABASE_URL=postgresql://etl_user:etl_pass@localhost:5432/healthcare_etl
PHI_ENCRYPTION_KEY=   # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENVIRONMENT=development
LOG_LEVEL=INFO
